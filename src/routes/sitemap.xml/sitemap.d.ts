type businessSiteMap = {
  title: string;
  slug: string;
  lastmod: string;
  priority: string;
  visible?: boolean;
};

type pagesSiteMap = {
  path: string;
  priority: string;
  changefreq: string;
};
